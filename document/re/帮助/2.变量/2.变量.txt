
    变量
    的意思就是可变的

    比如
    a = 1

    a 是可变的 可被赋值  计算完右边的结果赋值给 a


    变量读取是有优先级的
    获取变量 如果不存在则获取下一级的，我们称为第一变量域，如果不存在则获取第二变量域。如果不存在则获取第三。。如。。。
    设置变量 直接先从第一变量域中，如果不存在则获取第二变量域。如果不存在则获取第三。。如。。。

    变量的命名应该符合Java规范
    变量名开始不能是数字后面可以是字母数字下划线
